'use client';
import Link from 'next/link';
import { useRouter } from 'next/navigation';
import { ArrowLeft, ArrowRight, RotateCcw } from 'lucide-react';
import { PageTransition } from '@/components/page-transition';
import { Mascot } from '@/components/mascot/mascot';
import { useNemorra } from '@/components/app-provider';
import { overall, score } from '@/lib/learning';
export function ResultsScreen() {
  const { current, startSession, storageError } = useNemorra(); const router = useRouter();
  if (!current?.teach || !current.write) return <PageTransition><h1 className="page-title">A memory in progress.</h1><p className="page-subtitle mt-5">Complete Teach and Write to see your real feedback.</p><Link href={current ? current.teach ? '/write' : '/teach' : '/'} className="primary-button mt-7">Continue learning <ArrowRight size={18}/></Link></PageTransition>;
  const session = current;
  return <PageTransition><div className="flow-top"><Link href="/" className="icon-button outlined" aria-label="Back home"><ArrowLeft size={19}/></Link><span className="quiet-note">Session complete</span></div><h1 className="page-title text-balance" style={{fontSize:43}}>Here&apos;s what stuck.</h1><p className="page-subtitle mt-4">{session.material.title}</p><div className="flex items-center justify-center py-7"><div><p className="result-score">{overall(session)}<span className="text-4xl">%</span></p><p className="page-subtitle mt-2">A little more yours.</p></div><Mascot state="celebrating" className="feedback-mascot"/></div><div className="results-breakdown">{[['Teach', score(session.teach)], ['Write', score(session.write)], ['Overall', overall(session)]].map(([label, value]) => <div className="text-center" key={label}><p className="quiet-note">{label}</p><p className="mt-2 text-2xl font-semibold">{value}%</p></div>)}</div><p className="quiet-note mt-4">AI practice estimates. Each concept earns full, half, or no credit; overall is the mean of Teach and Write.</p><p className="page-subtitle mt-6">{session.write?.summary}</p>
    {(['remembered', 'partial', 'missing'] as const).map(status => <section className="mt-7" key={status}><h2 className="text-xl font-semibold">{status === 'remembered' ? 'Remembered' : status === 'partial' ? 'Almost there' : 'Needs another look'}</h2><div className="flex flex-col gap-3 mt-4">{session.write?.items.map((item, i) => item.status === status && <article className="account-card" key={i}><h3 className="font-semibold">{session.material.concepts[i]}</h3><p className="quiet-note mt-2">{item.feedback}</p></article>)}{!session.write?.items.some(item => item.status === status) && <p className="quiet-note">No concepts in this group.</p>}</div></section>)}
    <details className="account-card mt-7"><summary className="cursor-pointer font-semibold">Compare Teach feedback</summary><p className="page-subtitle mt-3">{session.teach?.summary}</p>{session.teach?.items.map((item, i) => <p className="quiet-note mt-3" key={i}><strong>{session.material.concepts[i]}</strong> — {item.status}: {item.feedback}</p>)}</details>
    <div className="mt-7 flex flex-col gap-3"><p className="quiet-note text-center" role="status">{storageError ? 'Not saved to this browser. Export from Your corner.' : 'Saved on this device. No cloud backup.'}</p><Link href="/recordings" className="primary-button">Your sessions <ArrowRight size={18}/></Link><button className="pill-button" onClick={() => { startSession(session.material); router.push('/teach'); }}><RotateCcw size={18}/>Practice again</button><Link href="/" className="pill-button">Learn something new</Link></div></PageTransition>;
}
